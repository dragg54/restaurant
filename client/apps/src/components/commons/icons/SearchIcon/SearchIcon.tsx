import styled from 'styled-components'

export const SearchIcon = styled.div`
  height: 40px;
  width: 30px;
  position: absolute;
  top:50%;
  transform: translate(10%, -40%);
  right: 12px;
  color: gray;
  &:hover{
    cursor: pointer
  }
`